class SlidingPiece

  def moves
    #you have position of piece, its directions, and board
    # valid_moves = []
    # [[0,1], [1, 1] etc].each |dir|
    # x = position[0]+ dir[0], y = position[1] + dir[1]
    # valid_moves << [x, y] unless board[x, y].color == self.color
    # if board[x, y].color == othercolor then next  



  end

end
